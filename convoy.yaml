language: java
appGroup: tessell
version: 1.0.3
generates:
  artifacts:
    - name: prometheus4j
      excludeFromReleaseManifest: true
      extension: jar
ci:
  bestPractices:
    jiraValidation: 
      jiraIdSource: BRANCH_NAME_PREFIX
      matchingRule: ALL
